fvdSpeedDial.Config = {
	
	FANCY_SIDE_DIALS_MAX_SCALE: 1.3,
	DISPLAY_AD_EVERY: (Math.floor(Math.random() * 3) + 3) * 24 * 3600 * 1000, // 3 - 5 days
  FS_DIALS_PREVIEW_DIR: "sd_previews",
  FS_MOSTVISITED_PREVIEW_DIR: "mostvisited_previews",
  FS_MISC_DIR: "misc",
  AUTOUPDATE_PREVIEW_ENABLED: true
	
};
